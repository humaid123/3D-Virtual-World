

    // **** Earth transform
    Transform earth_M = Transform::Identity();

    // calculate the earth's orbit as an ellipse
    float x_earth_orbit = 0.7*std::cos(-freq/EARTH_ORBITAL_PERIOD);
    float y_earth_orbit = 0.5*std::sin(-freq/EARTH_ORBITAL_PERIOD);

    //TODO3: translate the earth along earth's orbit using Eigen::Translation3f().
    earth_M *= Eigen::Translation3f(x_earth_orbit, y_earth_orbit, 0.0f);
    //save the earth's transform before spinning, so we don't spin the moon with the earth!
    Transform earth_M_prespin = earth_M;

    // make earth rotate around z-axis
    earth_M *= Eigen::AngleAxisf(-freq/EARTH_ROT_PERIOD, Eigen::Vector3f::UnitZ());

    // scale earth
    earth_M *= Eigen::AlignedScaling3f(0.08f, 0.08f, 1.0f);


// get the matrix back with
earth_M.matrix()